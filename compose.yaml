services:
  hwk-activity:
    build: .
    restart: unless-stopped
    init: true
    env_file: .env
    environment:
      DATABASE_PATH: /data/activity.sqlite
    volumes:
      - activity-data:/data
volumes:
  activity-data:
